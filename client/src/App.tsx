import StickyBoard from "@/components/StickyBoard";

function App() {
    return (
        <main className="min-h-screen bg-white">
            <StickyBoard />
        </main>
    );
}

export default App;
